# New Users Start With Empty Overlays

New users will receive a Personal List that begins as an Empty Overlay over the Canonical List rather than a copied set of canonical rows. This makes signup cheap, preserves ongoing canonical updates, and stores user data only when they actually diverge from the default schedule.
