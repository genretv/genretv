# One Personal List With Local View Preferences

Each user will have one Personal List in the MVP. Filters, ordering, and freeform Sub-Lists are modeled as View Preferences stored locally in the browser for both signed-in and anonymous visitors, avoiding multiple synced lists while still allowing users to shape what they see.
