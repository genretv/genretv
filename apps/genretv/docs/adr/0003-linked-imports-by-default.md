# Linked Imports by Default

Imports from public lists will default to Linked Imports, with Detached Copy available as an explicit management action. This preserves the value of maintainer updates for ordinary users while still supporting independent copies when a user wants to diverge fully from the source list.
